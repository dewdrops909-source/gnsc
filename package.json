{
  "name": "gnsc-tracker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "GNSC Vehicle Tracker — test harness for cross-device time & sync logic",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
